# スマホから開発を続ける

外出先でもスマホから Claude Code に指示を出して、このアプリを直せるようにするための手引き。

---

## 使うもの

**Claude Code（Web版）** — スマホのブラウザで `claude.ai/code` を開く。
Claude アプリからも入れる。専用アプリの導入や、PCの電源を入れっぱなしにする
必要はない。**手元のPCとは別に、クラウド上の作業環境が立ち上がる。**

初回だけ GitHub との接続を許可し、リポジトリに `toriumib/nanimonjya` を選ぶ。

---

## スマホからできること / できないこと

| やりたいこと | できる | 補足 |
|---|:---:|---|
| コードを直す・機能を足す | ✅ | いつもどおり日本語で指示すればよい |
| `flutter analyze` / `flutter test` | ✅ | クリーンなクローンで234件通ることを確認済み |
| `flutter build web` | ✅ | 同上 |
| GitHub へ commit / push | ✅ | |
| STORY.md など資料の編集 | ✅ | |
| **Vercel へデプロイ** | ⚠️ | トークンが要る（下記） |
| **AAB のビルド（署名）** | ❌ | **署名鍵は手元のPCにしか無い。これは意図的** |
| 実機での動作確認 | ❌ | Web版のURLを開いて見るのが代わりになる |

---

## ⚠️ AAB だけは、スマホからは出せない

署名鍵 `key.jks` は、リポジトリから**わざと外してある**（履歴からも削除済み）。
クラウドの作業環境には鍵が無いので、リリース用のAABは作れない。

これは不便だが**そのままにしておくこと**。鍵をリポジトリに戻すと、
リポジトリを見られる人が誰でもこのアプリの署名を作れるようになる。

→ **スマホでは「コードを直して push まで」**。
　 AAB は家に帰ってから、PCで `scripts/bump_and_build.ps1` を回す。

---

## Vercel へスマホからデプロイしたい場合

クラウド環境には Vercel のログイン情報が無いので、
**アクセストークンを1回だけ用意**する。

1. PCで https://vercel.com/account/tokens を開き、トークンを作る
2. Claude Code（Web版）の環境変数に `VERCEL_TOKEN` として設定する
3. 以降はこう頼めばデプロイできる:

```bash
cd build/web && npx vercel --prod --token=$VERCEL_TOKEN --yes
```

⚠️ トークンは**パスワードと同じ**。チャットに直接貼らず、環境変数に入れる。
漏れたと思ったら上のページからすぐ失効させる。

---

## スマホから頼むときのコツ

- **画面の名前で言う**と伝わりやすい（「顔メモの」「マイページの」「ホームの」）
- 迷ったら `CLAUDE.md` を読んでもらう。アプリの決まりごとが全部書いてある
- 確認は Web版のURLで:
  **https://web-sigma-drab-72.vercel.app**

### そのまま使える頼みかたの例

> ホームの「レビューする」ボタンの色を、もう少し目立たせて。
> テストとanalyzeを通してから push して。

> 顔メモの髪型に「アフロ」を足して。
> avatar.dart の hairCount と kHairJa、avatar_view.dart の描画の3か所ね。

> STORY.md の6.3を読んで、桐生の台詞案を5つ出して。まだコードは触らないで。

---

## 家に帰ってからやること

スマホで push した内容をPCに取り込み、必要なら AAB を出す。

```bash
cd "C:/Users/tori/Downloads/nanimonjya-main/nanimonjya-main" && git pull
```

AAB とWebデプロイの手順は `DEPLOY.md` を参照。
