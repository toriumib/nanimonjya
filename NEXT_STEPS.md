# 今後のロードマップ（データに基づくステップ順）

> 2026-08-20 時点。データ出所: GA4 エクスポート（2026-07-22〜08-18）・Crashlytics・AdMob・実装状況。
> 詳細な分析は [Analysis.md](Analysis.md) を参照。

---

## 🩹 直近の対応（2026-08-20 追加）

**報告された問題 → 対応状況**

| 問題 | 対応 |
|---|---|
| 黒基調テーマで黒文字が見にくい | ✅ テーマを白基調に戻し、文字を黒に（`app_style.dart` / `main.dart`） |
| BGM がおかしい（再生ボタンで鳴らない・ランダム） | 🟡 コード確認済み。エミュレータの無音は環境依存の可能性。実機確認が必要 |
| ショップでレイアウトエラー | 🟡 調査中。具体的なエラー（オーバーフロー箇所）の特定待ち |
| みんなで対戦の人数スライダー | ✅ 2〜12人に変更済み |

**BGM について（理解のためのメモ）**
- ホーム・試合中・リザルトの3場面で曲を別々に設定できる
- ホームは既定「おまかせ（ランダム）」なので、曲を選んでもホームではランダムに見える
  → マイページ「曲を選ぶ（ホーム）」で固定の曲に変更できる
- エミュレータはオーディオ出力がないと無音になる。実機で確認する

**ショップのレイアウト改善（未着手・候補）**
- [ ] IAP コインパック（500/1200/3000）を縦積みから横並びへ
- [ ] 「準備中」表示のボタンを、商品未登録時は非表示にしてスッキリさせる
- [ ] セクション間の余白・見出しを統一

---

## 📊 現状データの要約

| 指標 | 値 | 状態 |
|---|---|---|
| 起動→初回ゲーム転換 | 26%（203人→53人） | 🔴 74%が未プレイで離脱 |
| Day 7 リテンション | 全コホート 0% | 🔴 致命的 |
| app_remove | 新規の約28%が削除 | 🔴 致命的 |
| app_exception | 1699件（18人、1人約94件） | 🔴 要修正 |
| OOM クラッシュ | 別アプリで ExoPlayer（音声）リーク確認 | 🔴 nanimonjyaも要監査 |
| ストーリーモード | story_start=1（実質デッド） | 🟡 撤去検討 |
| 名刺覚え | recall_start=4（2人） | 🟡 導線が薄い |
| 広告収益 | GA4 に未連携（$0 表示） | 🔴 収益が見えない |
| IAP（コイン現金購入） | 実装済み・Play Console 商品登録済み | ✅ 実機で確認待ち |

---

## 🚀 ステップ1: クラッシュ・例外の撲滅（最優先）

**なぜ**: app_exception 1699件（18人）が最悪の離脱要因。起動→初回ゲーム26%の原因の一端。

- [ ] **1-1. 音声プレイヤー（ExoPlayer）リークの監査・修正**
  - `lib/services/bgm.dart` / `sfx.dart` の AudioPlayer が `dispose()` されずに増え続けると OOM（192MBヒープ枯渇）で落ちる
  - 別アプリ（com.kengo.marubatusokketu）の Crashlytics で同根の OOM を確認済み
  - 方針: プレイヤーは1つ持ち回し、曲切替は setAsset で差し替え。試聴後は必ず元の曲に戻す（既存の preview→restartCurrent を確認）
- [ ] **1-2. Crashlytics のスタックを画面別に集計**
  - `AppAnalytics.screen()` で画面名を Crashlytics に載せる対応は済み
  - 多発画面（上位3つ）を特定して、それぞれの例外を修正
- [ ] **1-3. 未捕捉エラーの握り潰し確認**
  - `main.dart` の初期化（IAP・広告・Firebase）が全て try/catch 済みか再確認
  - 非同期 rejection は `catchError` で必ず握る

**完了条件**: app_exception が月100件以下。

---

## 🚀 ステップ2: 初回体験の改善（26% → 50%）

**なぜ**: 起動した74%がゲームを1回も始めずに消える。リテンション・収益すべての根本。

**すでに実施済み**:
- ✅ 初回「さあ、はじめよう！」画面を廃止（サイレント付与）
- ✅ チュートリアルを「見る/見ない」選択式に
- ✅ ホームに大きな「あそびかた」ボタン
- ✅ 「みんなで対戦」「ひとりで対戦」を大きく表示

**次の一手**:
- [ ] **2-1. 初回の3タップでゲーム開始** を計測で確認（`first_game_started` の秒数分布）
- [ ] **2-2. チュートリアル「見る」選択率の計測**（選択結果をイベント化）
- [ ] **2-3. 初回のコイン・キャラ付与をホームで見せる**（サイレント付与だと「何がもらえたか」が伝わらない可能性）

---

## 🚀 ステップ3: リテンションの改善（Day7 0% → 8%）

**なぜ**: 1週間後に戻る人が0人。通知・復習・毎日の理由が無い。

- [ ] **3-1. 通知のソフトアスクを「初回ゲーム完了直後」に出す**
  - 現状: notify_prompt が9件しか出ていない（ゲーム1回完了後に限定的）
- [ ] **3-2. 復習キュー（spaced review）をホームの先頭に常設**
  - 「この人、また会わない？ 日をあけてもう一度」の導線
- [ ] **3-3. デイリーボーナス・ストリークをホームで目立たせる**
  - 「あとN日で新キャラ」の進行度
- [ ] **3-4. 週間レポート通知**（「今週は◯人覚えました！」）

---

## 🚀 ステップ4: 収益化の見える化（AdMob 収益を GA4 へ）

**なぜ**: イベント表の「合計収益 $0」は、収益が GA4 に連携されていないため。儲かっているか判断不能。

- [ ] **4-1. 広告の `onPaidEvent` で `ad_impression`（value付き）を送る**
  - banner / interstitial / rewarded すべて
- [ ] **4-2. IAP の購入イベントを確認**
  - Play Console 商品登録済み。実機で「コイン現金購入」が動くか検証
  - `shop_purchase(method: iap)` が記録されるか
- [ ] **4-3. 広告の出し方を再点検**
  - バナー自動リフレッシュの水増しは修正済み（初回ロードのみ adShown）
  - 低トラフィック画面のバナーは外す判断

---

## 🚀 ステップ5: 機能の整理（リソースを集中）

**なぜ**: 使われていない機能に保守コストをかけ続けている。

- [ ] **5-1. ストーリーモード（ものがたり）の撤去**
  - story_start=1 と実質デッド。Noah ストーリー（英語未翻訳）を撤去し、読み物に集中
  - 既にタブからはマイページに格下げ済み
- [ ] **5-2. 名刺覚え（ビジネス特訓）の導線強化 or 整理**
  - recall_start=4（2人）と未活用。マイページのボタンのみだと見つからない
  - 「初回のホーム」か「顔メモ」と統合するか判断
- [ ] **5-3. デッドコード・未使用イベントの整理**
  - tap_action 等の未配線イベントは配線するか削除

---

## 🚀 ステップ6: 計測の整備（次の判断をデータで）

**なぜ**: 改善の効果を測れないと、次の一手が決められない。

- [ ] **6-1. GA4 カスタムディメンション登録**
  - `mode` / `feature` を登録しないと、モード人気の内訳が出ない（要コンソール作業）
- [ ] **6-2. 未配線イベントの確認**
  - `answer` は配線済み（2026-08-20）
  - `tap_action` は任意
- [ ] **6-3. チュートリアル選択・名刺覚えへの導線にイベントを追加**

---

## 📅 実施の順序（1週間目安）

| 週 | やること |
|---|---|
| 今週 | ステップ1（クラッシュ修正）→ ステップ4-1（収益連携） |
| 来週 | ステップ2（初回体験）→ ステップ3-1,3-2（通知・復習） |
| 再来週 | ステップ5（機能整理）→ ステップ6（計測整備） |

> ⚠️ 各ステップ完了後は必ず `flutter analyze` / `flutter test`（全295件）を通過させ、Web（Vercel）と AAB を配信する。
