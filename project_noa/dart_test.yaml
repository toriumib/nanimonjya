# 🎨 絵の一覧（test/art_sheet_test.dart）は、ふだんのテストでは走らせない。
#
# ゴールデン画像はフォントやGPUの違いで1ドット単位でずれるので、
# 別の環境で `flutter test` すると中身が正しくても落ちる。
# あれは「正しさを判定するもの」ではなく**目で見るために書き出すもの**なので、
# 見たいときだけ手元で明示的に回す:
#
#   flutter test --update-goldens --tags art
#
tags:
  art:
    skip: "絵の一覧。見たいときだけ --tags art で回す"
