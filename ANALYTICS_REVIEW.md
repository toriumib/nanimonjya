# アクセス解析レビュー（2026-07-12 〜 08-08）

Firebase / GA4 / AdMob のエクスポートを読んだ結果と、次にやること。
実装リファレンスは [ANALYTICS.md](ANALYTICS.md)。こちらは**この期間の所見と改善計画**。

⚠️ 母数が小さい（新規182人・28日）。**比率は方向を見るだけに使い、
断定に使わない。**とくに AdMob の eCPM は表示14回から出た数字なので、
レートとしては意味がない。

---

## 0. 全体像（3行）

- **入口は動いている。中身に入れていない。** 新規162人のうち、下タブを1度でも切り替えた人は**17人（10%）**
- **1週間後にほぼ全員いない。** 直近コホートの週1維持率は **3〜5%**
- **収益は $6.08。その96%がリワード広告**。ただしリワードは**446回ロードして14回しか出していない**

---

## 1. 数字

### 1.1 ユーザー

| 指標 | 値 |
|---|---|
| 新規ユーザー（Android） | 162 |
| 新規ユーザー（Web） | 20 |
| 国 | 日本 114／不明 53／その他 9 |
| 都市 | 横浜 26・大阪 7・千代田 4・港 4・新宿 4 |
| 流入 | Direct 102 ／ Organic Search 80 |
| 日本のユーザーの平均エンゲージメント時間 | 892秒（約15分） |
| 1日アクティブ | 2〜34人（ならすと7〜10人） |

⚠️ 横浜26人は開発者の周辺の可能性がある。**自分の端末とテスト端末を
GA4 の内部トラフィックから除外していないと、以後ずっと数字が歪む。**

### 1.2 ファネル（ユーザー数ベース）

| 段階 | ユーザー | 直前からの残り |
|---|---|---|
| first_open | 162 | — |
| session_start | 163 | — |
| game_start | 75 | **46%** |
| game_end | 37 | **49%** |
| feature_open（タブ切替） | 17 | **10%** |

**落ちているのは2か所。**

1. **起動 → ゲーム開始で半分落ちる。** 84人がゲームを1回も始めずに消えている
2. **タブに触るのが10%。** 顔メモ・名刺覚え・よみもの・ものがたりは、
   そもそも**見られていない**。機能の良し悪し以前の問題

game_start 229回に対し game_end 90回。**完走率39%**。始めても6割が最後まで行かない。

### 1.3 維持率（週次コホート）

| コホート | 0週 | 1週 | 2週 | 3週 |
|---|---|---|---|---|
| 7/12–18 | 51 | 17（**33%**） | 7 | 7 |
| 7/19–25 | 37 | 2（**5%**） | — | — |
| 7/26–8/1 | 58 | 2（**3%**） | — | — |

**最初のコホートだけ維持率が10倍高い。**
同じ期間のバージョン分布を見ると、7/12–18 はほぼ **1.2.0**、
以降が 2.2.0 → 2.3.0 → 2.4.0 → 2.5.0。

> 仮説：**2.x のピボットで維持率が落ちた。**
> ただし n が小さく、初期コホートに知人が多い可能性もある。
> 断定はできない。**次の2コホートで再現するか見る。**

アンインストールは `app_remove` 47人＝新規162人の **29%**。

### 1.4 機能別の利用（分かる範囲）

| 機能 | イベント | 回数 / ユーザー |
|---|---|---|
| なまえコール | `namecall_progress` | 91 / 17 |
| CPU対戦 | `cpu_match_end` | 24 / 13 |
| 一人特訓 | `solo_training_end` | 5 / 3 |
| デイリーボーナス | `daily_bonus_claimed` | 9 / 3 |
| ショップ | `shop_item_tap` | 2 / 1 |
| モード選択 | `mode_pick` | 11 / 1（※実装が 8/8。まだ配信前） |

### 1.5 ⚠️ 計測されていない機能

**「使われていない」のか「測っていない」のかが区別できていない。**
イベントが1件も無いのは、以下のすべて。

| 機能 | 状態 |
|---|---|
| 顔メモ（`custom_roster`） | 専用イベントなし |
| 似顔絵エディタ（`avatar_editor`） | 専用イベントなし |
| 名刺覚え／思い出し訓練（`recall_training`） | 完了イベントが `solo_training_end` と混在 |
| ものがたり（`story_noah`） | `story_progress` は実装済みだが**発火0件** |
| よみもの（`memory_tips` / `article_library`） | 専用イベントなし |
| キャラデッキ | 専用イベントなし |
| オンライン各種 | `online_match_end` 実装済み・発火0件 |

さらに実装側で見つけた問題:

- **`AppAnalytics.spacedReviewDone` は呼び出し元がゼロ。** 死んだAPI
- ~~`mode_pick` が1ユーザーしか発火していない~~ → **実装が 2026-08-08 で、
  まだ配信に乗っていないだけだった**（`bb9e047`）。異常ではない。
  次のリリース後にあらためて見る
- **`feature_open` は下タブの切替でしか撃っていない**
  （[home_shell.dart:117](lib/screens/home_shell.dart:117)）。
  ホームのタイル（顔メモ・チュートリアル等）からの遷移は数えていない

### 1.6 ⚠️ screen_view は「壊れている」のではなく「見る列を間違えている」

エクスポートされた表は **「ページタイトルとスクリーンクラス」** で、
Flutter アプリは画面が全部 `MainActivity` の中にあるので
**必ず MainActivity（755）になる**。これは正常。

コード側は24画面で `logScreenView(screenName:)` を呼んでいるので、
**`screen_name` のほうにはデータがあるはず**。

> GA4 →「ページとスクリーン」→ ディメンションを
> **「スクリーン名」（screen_name）** に変えて見る。
> それでも `(not set)` ばかりなら、そのとき初めて実装を疑う。

登録済みの screen_name（24種）:
`top` `player_selection` `cpu_entry` `match_result` `local_result`
`online_lobby` `online_result` `ranking` `line_match` `recall_training`
`training_hub` `training_report` `report` `study` `custom_roster`
`avatar_editor` `character_deck` `shop` `memory_tips` `article_library`
`story_noah` `story_legacy` `profile` `tutorial`

### 1.7 不具合

| 指標 | 値 |
|---|---|
| `app_exception` | 317件 / 11ユーザー（**1人あたり28.8件**） |
| クラッシュ非遭遇率（8/7） | 85.7% |

11人に集中しているので、**特定の端末か特定の画面**で
ループして出ている可能性が高い。Crashlytics のスタックを先に見る。

---

## 1.8 深掘り — 前回見落としていた4つ

### ① 試合の44%が「消えて」いる

| 事象 | 回数 |
|---|---|
| `game_start` | 229 |
| `game_end`（完了） | 90 |
| `game_exit`（自分でやめた） | 37 |
| **どちらでもない** | **102（44.5%）** |

完了もせず、やめる操作もしていない試合が102ある。
つまり**アプリごと落ちたか、途中で放置されて閉じられた**。

同じ期間の `app_exception` が317件。**この2つは無関係ではない可能性が高い。**
「完走率39%」を1ゲームの長さの問題だと思っていたが、
**半分近くは落ちているだけかもしれない。**

⚠️ **ここを確かめずにゲーム設計を変えても意味がない。**
Crashlytics を先に見る。

### ② 46%が「エンゲージメント」にすら到達していない

`user_engagement` は88ユーザーにしか発火していない（`session_start` は163）。
このイベントは「10秒以上の滞在または2画面以上」で出るので、
**75人（46%）は数秒で閉じている**。

セッションは1人あたり **1.45回**。ほとんどの人が1回開いて終わり。
「1週間後にいない」以前に、**1分後にいない**。

### ③ お金と再訪に関わる導線が、全部ひとケタ

| 導線 | 触った人 | 全体比 |
|---|---|---|
| リワード広告を1回でも見た | 8人 | 4.9% |
| デイリーボーナスを受け取った | 3人 | 1.9% |
| 通知の同意画面まで来た | 5人 | 3.1% |
| **ショップを触った** | **1人** | **0.6%** |

**再訪の仕掛けが全部、誰にも届いていない。**
デイリーボーナスも通知もストリークも実装済みなのに、
**そこへ辿りつく前に消えている**。

ひとつだけ良い数字がある。**リワード広告を出せたときの完走率は65%**
（20回表示 → 13回最後まで視聴）。
**見せさえすれば見てもらえる。**問題は8人にしか出せていないこと。

### ④ `game_start` にはチュートリアルが混ざっている

おためしチュートリアルも `game_start(mode: 'tutorial_play')` を撃つ。
つまり **75人の「ゲーム開始」の多くは、強制的に出ていたチュートリアル**
かもしれない。

⚠️ そうだとすると、**自分の意思で遊んだ人はもっと少ない。**
`mode` をカスタムディメンションに登録するまで、この切り分けはできない。
**4.3 の設定を先にやること。**

### ⑤ そのほか、気になる小さな数字

| 事実 | 読み |
|---|---|
| 国が `(not set)` のユーザーが53人（29%） | 起動直後に閉じて国判定が終わっていない |
| `app_clear_data` 4人 | 「おかしくなったから消した」の典型。少数だが不具合の合図 |
| `app_update` が12人 | **リリースを跨いで残っている人が12人しかいない** |
| 画面/人 6.0 に対しゲーム開始75人 | 画面は見ている。**遊ぶところまで行けていない** |
| 日本のユーザーの平均滞在892秒 | ⚠️ N日目13〜14が3,110秒/3,362秒。**開発端末が混ざっている** |

---

## 1.9 モード別の人気（分かる範囲）

⚠️ **正確な順位は出せない。** `game_start` の `mode` パラメータを
GA4 のカスタムディメンションに登録していないので、絞り込めない。
下は「完了イベント」から間接的に見たもの。

| 順 | モード | 手がかり | 回数 / 人 | 状態 |
|---|---|---|---|---|
| 1 | **なまえコール** | `namecall_progress` | 91 / 17 | 唯一まともに遊ばれている |
| 2 | CPU対戦 | `cpu_match_end` | 24 / 13 | 1人あたり1.8回。続かない |
| 3 | 一人特訓 | `solo_training_end` | 5 / 3 | ほぼ使われていない |
| — | オンライン各種 | `online_match_end` | **0 / 0** | **到達者ゼロ** |
| — | ものがたり | `story_progress` | **0 / 0** | **到達者ゼロ** |
| — | ランクマッチ | （専用イベント無し） | 不明 | — |
| ? | 顔メモ | （イベント無し） | **測っていない** | 不明 |
| ? | よみもの | （イベント無し） | **測っていない** | 不明 |
| ? | キャラデッキ | （イベント無し） | **測っていない** | 不明 |

**読み取れること**

- **なまえコール1本で持っている。** ほかは実質ゼロ
- **オンラインとものがたりは、実装済みのイベントが0件**。
  作ったのに誰も辿りついていない。タブ到達率10.5%と整合する
- 顔メモ・よみものは**判定不能**。次のリリースで初めて分かる
- CPU対戦は1人あたり1.8回。**2回目で飽きている**

⚠️ **「使われていないから消す」判断を、いまの数字でしてはいけない。**
到達率が10%では、機能の良し悪しを測れていない。
まず**見てもらう**、それから良し悪しを測る。

---

## 2. 広告（AdMob）

| フォーマット | 収益 | リクエスト | 表示 | 表示率 | クリック |
|---|---|---|---|---|---|
| **リワード** | **$5.82** | 446 | 14 | **3.14%** | 2（14.3%） |
| バナー | $0.16 | 788 | 646 | 95.0% | 4（0.62%） |
| インタースティシャル | $0.10 | 56 | 7 | **12.5%** | 1 |
| リワードインタースティシャル | $0.00 | 14 | **0** | **0%** | 0 |
| 合計 | **$6.08** | | | | |

### 2.1 読み取れること

**1表示あたりの価値が桁違いに違う。**

| フォーマット | 1表示あたり |
|---|---|
| リワード | 約 **$0.42** |
| バナー | 約 **$0.00025** |

リワード1回は**バナー約1,700回ぶん**。
（⚠️ リワードは14表示しかないので、この倍率は誤差が大きい。
ただし「リワード ≫ バナー」という順序自体は業界的にも一貫している）

### 2.2 いちばん大きい損失は「ロードしたのに出していない」

- リワード：**446ロード → 14表示（3.1%）**
- インタースティシャル：**56ロード → 7表示（12.5%）**
- リワードインタースティシャル：**14ロード → 0表示**

ロードだけしてクリックされない状態が続くと、
**AdMob 側の評価（マッチ率・単価）にも効いてくる。**
「先読みしすぎ」か「表示条件が厳しすぎ」のどちらか。

### 2.3 広告設計の作り直し（利益最大化）

#### 前提：1表示の価値が3桁ちがう

| フォーマット | 収益 | 表示 | **1表示あたり** |
|---|---|---|---|
| リワード | $5.82 | 14 | **$0.4157** |
| インタースティシャル | $0.10 | 7 | $0.0143 |
| バナー | $0.16 | 646 | $0.0002 |

**リワード1回 ＝ インタースティシャル29回 ＝ バナー1,700回。**

⚠️ リワードは14表示しかないので、この倍率自体は誤差が大きい。
ただし桁が3つ違うので、**順位が入れ替わることはまず無い**。

結論は単純で、**やることはリワードの表示回数を増やすことだけ**。
バナーを増やしても、インタースティシャルを増やしても、
体感の悪化に見合うリターンが無い。

#### 何をどこに出すか

| 面 | 出すもの | 方針 |
|---|---|---|
| ゲーム中 | **何も出さない** | 誤タップはポリシー違反リスク。1表示$0.0002のために負うリスクではない |
| 画面下（常設） | バナー | **現状維持。増やさない** |
| 結果画面 | インタースティシャル（3プレイに1回） | 現状維持。ただし**結果を見せてから**出す（実装済み） |
| 結果画面 | **リワード「コイン2倍」** | いちばんの稼ぎ頭。獲得コイン0のときは出さない（実装済み） |
| ショップ | **リワード「動画でコイン+60」** | 既存 |
| コインが足りないとき | **リワード** | 既存（`shop_short_of_coins`）。**買いたい気持ちが最高潮の瞬間**なので、いちばん効く |
| よみもの | リワードインタースティシャル | **廃止して通常リワードに寄せる**（下記） |

#### 変えたこと（実装済み）

| # | 内容 | 効果の見込み |
|---|---|---|
| 1 | **リワードの在庫を共有ひとつにした** | 446ロード→**表示のたびに1ロード**。空振りがほぼ消える |
| 2 | 画面を閉じてもロード済み広告を**捨てない** | いちばんの原因。5画面が各自ロードして各自捨てていた |
| 3 | 起動時の全画面広告の先読みを**やめた** | インタースティシャル56ロード/7表示、リワードインタースティシャル14ロード/0表示 |
| 4 | インタースティシャルは**あと1プレイで出る時点**で読む | 同上 |
| 5 | 期限切れ（50分）を捨てて読み直す | 古い在庫を出そうとして失敗するのを防ぐ |
| 6 | `ad_load_requested` / `ad_shown` / `ad_skipped(reason)` を全フォーマットに | 次は原因が数字で分かる |

#### これから増やすリワードの出番（未実装）

**「見たら得する」ではなく「見ないと損する」に寄せる**ほうが視聴率は高い。

| 置き場所 | 見返り | なぜ効くか |
|---|---|---|
| まちがえた直後 | **1回だけ正解あつかい**（お守り「まちがえ守り」の1回付与） | 損を取り返せる瞬間。いちばん押される |
| 「今日のキャラ」を引いたあと | もう1体引ける | 引いた直後は期待値が高い |
| 顔メモの登録後 | 名刺スキンを1日開放 | 作ったものを飾りたい心理 |
| 連続日数が途切れそうなとき | ストリーク保護 | 積み上げを守りたい |

⚠️ **1セッションあたりの上限を決めること（3回まで）。**
上限が無いと、見境なく出して嫌われる。
リワードは「押した人だけが見る」形式なので、
上限さえ守れば体験を壊さない。

#### 期待値の試算

いまは **$6.08 / 28日 ＝ 1日$0.22**。

リワードの表示率が 3.1% → 30%（在庫共有で空振りが消えるぶん）になり、
出番が4か所増えて 1人あたり 0.1回/日 → 0.4回/日 になったとする。

> DAU 10人 × 0.4回 × $0.42 ＝ **1日$1.68**（いまの約8倍）

⚠️ それでも**月$50に届かない**。
広告の作りをどれだけ直しても、**DAU 10人では上限がここ**。
本命は3章の維持率で、広告はその後に効いてくる。

---

## 3. DAU を伸ばす

効果と手間で並べた。上から順に。

### 3.1 まず塞ぐ穴（新機能より先）

| # | 施策 | 根拠 |
|---|---|---|
| 1 | **起動直後に1ゲーム遊ばせる**。設定・チュートリアルを挟まない | 起動した162人のうち87人がゲームを始めていない |
| 2 | **タブの存在に気づかせる**。初回のみタブを指すコーチマーク、または初回だけ「顔メモ」を先頭に出す | タブを触った人が10% |
| 3 | **1ゲームを短くする**。または途中終了でも結果を出す | 完走率39% |
| 4 | **app_exception 317件を潰す** | 11人に集中。その11人はまず戻ってこない |
| 5 | **1.2.0 と 2.x で何が変わったか調べる** | 維持率が33%→3%。ここが本丸の可能性 |

### 3.2 戻ってくる理由を作る

| # | 施策 | 備考 |
|---|---|---|
| 6 | **間隔反復（spaced repetition）を主役にする** | 顔メモに登録した人を「明日また出す」。`spacedReviewDone` は実装済みなのに**呼ばれていない**。ここを繋ぐだけで再訪理由になる |
| 7 | **通知を意味のあるものに** | いまは19時の定時通知。「昨日まちがえた3人が待っています」に変える |
| 8 | **デイリーボーナスの動線を強くする** | 3人しか受け取っていない |
| 9 | **連続日数（ストリーク）を見せる** | スタンプカレンダーは実装済み。ホームの一等地に出す |

### 3.3 流入を増やす

| # | 施策 | 備考 |
|---|---|---|
| 10 | **ストア掲載文の差し替え** | [STORE_LISTING.md](STORE_LISTING.md) に用意済み。Organic Search が80人＝すでに検索から来ている。ここは伸びしろ |
| 11 | **スクショの1枚目を似顔絵エディタに** | `store_assets/screenshots/` に用意済み |
| 12 | **Web版の導線** | Web 20人／223ビュー。ブラウザで遊んでインストールに繋げる導線が無い |

---

## 4. これからのイベント設計

**目的：「使われていない」と「測っていない」を二度と混同しない。**

### 4.1 命名の決まり

- `snake_case`、名詞＋動詞の過去形（`face_memo_added`）
- **機能ごとに「開いた」「使った」「終えた」の3点を撃つ。**
  開いた数だけでは、使われたのか眺めて閉じたのか分からない
- パラメータは**固定の識別子**のみ。自由入力・個人情報は絶対に載せない
- GA4 のイベント名は**上限500種**。機能ごとにイベントを増やすより、
  1イベント＋パラメータで分ける

### 4.2 追加するイベント

#### 顔メモ（いま完全に見えていない）

| イベント | パラメータ | いつ |
|---|---|---|
| `face_memo_open` | `from`（home/tab/deck） | 画面を開いた |
| `face_memo_add_start` | `kind`（avatar/photo/camera） | 追加方法を選んだ |
| `face_memo_added` | `kind`, `fields_filled`（0-18） | 登録が完了した |
| `face_memo_study` | `count` | おぼえるを開始 |
| `face_memo_quiz_end` | `count`, `correct` | かくにんテスト終了 |
| `avatar_editor_done` | `changed`（いじった項目数） | 「この顔で決定」 |

**`face_memo_add_start` と `face_memo_added` の差が離脱率**になる。
登録フォームが18項目もあるので、ここは必ず測る。

#### 名刺覚え

| イベント | パラメータ | いつ |
|---|---|---|
| `recall_start` | `level`, `fields`（出題項目数） | 「出会いに行く」 |
| `recall_meet_done` | `count` | であうフェーズ終了 |
| `recall_end` | `count`, `correct`, `fields` | 結果表示 |

#### ものがたり・よみもの

| イベント | パラメータ | いつ |
|---|---|---|
| `story_progress` | `phase` | **実装済み。発火しているか確認する** |
| `story_ending` | `ending`, `correct`, `total` | **実装済み** |
| `read_open` | `article_id` | 読み物を開いた |
| `read_page` | `article_id`, `page` | ページ送り |
| `read_finish` | `article_id` | 最終ページ |

`read_page` があると**何ページ目で閉じられるか**が分かる。全25話あるので必須。

#### 復習・再訪

| イベント | パラメータ | いつ |
|---|---|---|
| `spaced_review_due` | `count` | 復習対象が発生 |
| `spaced_review_done` | `total`, `correct` | **実装済み・呼び出し元ゼロ。繋ぐこと** |
| `notification_open` | `kind` | 通知から起動 |

#### 広告

| イベント | パラメータ | いつ |
|---|---|---|
| `ad_load_requested` | `format`, `placement` | ロード開始 |
| `ad_shown` | `format`, `placement` | 実際に表示 |
| `ad_skipped` | `format`, `placement`, `reason` | 出せなかった |

**`ad_load_requested` と `ad_shown` の差**が、いま起きている
「446ロード14表示」の原因を特定する唯一の手段。`reason` に
`not_loaded` / `cooldown` / `no_fill` / `user_closed` を入れる。

#### 導線

| イベント | パラメータ | いつ |
|---|---|---|
| `mode_pick` | `mode` | **実装済み。配信後に確認する** |
| `feature_open` | `feature`, `from`（tab/tile/deeplink） | **`from` を追加**。いまタブ切替しか数えていない |
| `first_game_started` | `seconds_since_first_open` | 初回ゲーム開始 |

`first_game_started` は 3.1 の①を測るための指標。
**初回起動から何秒でゲームに入れたか**が、いちばん改善効果が大きい。

### 4.3 GA4 側の設定（コードを書く前に）

1. **内部トラフィックの除外。** 開発端末の IP / インスタンスIDを除外しないと、
   横浜26人のような数字を信じてしまう
2. **カスタムディメンションの登録。** GA4 は登録していないパラメータで
   絞り込めない。`mode` `feature` `kind` `format` `placement` `article_id`
   `ending` を**イベントスコープのカスタムディメンションとして登録する**
3. **キーイベント（旧コンバージョン）の設定。** いまは `first_open` だけ。
   `face_memo_added` `recall_end` `spaced_review_done` を追加する
4. **BigQuery エクスポートを有効化。** 無料枠で足りる。
   GA4 の画面では見られない「同じ人が何をどの順で触ったか」が追える

### 4.4 毎週見る数字（これだけ）

| 指標 | いまの値 | 目標 |
|---|---|---|
| 初回起動 → 初回ゲーム開始 | 46% | 70% |
| ゲーム完走率 | 39% | 60% |
| タブ到達率 | 10% | 40% |
| 週1維持率 | 3〜5% | 15% |
| リワード表示率 | 3.1% | 10% |
| app_exception / ユーザー | 28.8 | 1未満 |

---

## 5. 実装の順番

1. `app_exception` の原因特定（Crashlytics）
2. `ad_load_requested` / `ad_shown` / `ad_skipped` を入れる（原因が分からないと直せない）
3. 顔メモ・名刺覚え・よみもののイベントを入れる
4. `spaced_review_done` を実際に呼ぶ導線を作る
5. `feature_open` に `from` を足す
6. GA4 側の設定（4.3）
7. 起動直後に1ゲーム（3.1①）
8. ストア掲載文とスクショの差し替え

⚠️ **2〜6 が終わるまで、広告の配置をいじらない。**
いま動かすと、効果があったのか偶然なのか判別できない。
