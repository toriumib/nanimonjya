# ペタネーム データ分析と改善計画

**分析期間**: 2026-07-22 〜 08-18（28日間）
**データソース**: Firebase Analytics GA4 エクスポート / Crashlytics / AdMob

---

## 1. サマリ

| 指標 | 値 | 評価 |
|---|---|---|
| 新規ユーザー | 228人（Direct 82% / Organic Search 17% / Referral 0.4%） | 自然流入中心 |
| プラットフォーム | Android 227 / Web 25 | ほぼ Android |
| **起動→初回ゲーム転換** | **26%（203人→53人）** | 🔴 74%が未プレイで離脱 |
| **Day 7 リテンション** | **全コホート 0%** | 🔴 致命的 |
| **app_remove** | **64件（63人）＝新規の約28%が削除** | 🔴 致命的 |
| **app_exception** | **1699件（18人）** | 🔴 要修正 |
| ストーリーモード | story_start 1 / story_ending 1 | 実質1人＝デッド |
| 広告 | ad_shown 6095（15人＝1人406回） | ⚠️ バナー自動リフレッシュで水増し |
| 収益 | GA4上は $0（AdMob収益が未連携） | 🔴 収益が見えない |

---

## 2. 最優先で直す3つの問題

### 2.1 🔴 起動→初回ゲーム 26%（74%が一度も遊ばず離脱）

`first_open 203人 → first_game_started 53人（26%）`。

起動した人の**4分の3が、ゲームを1回も始めずに消えている**。これがリテンション・収益すべての根本。

**原因と対応**:
- ホームの「遊ぶ」導線が下に埋もれている → **初回起動は即ゲーム開始**（`home_shell.dart` の `_maybeShowTutorial` は既に実装済み：WelcomeGift → TutorialPlayScreen の流れ）。継続確認。
- 「あそびかた」への入口が小さい → **Home に大きな「あそびかた」ボタンを追加済み**（`top_screen.dart`）。

### 2.2 🔴 Day 7 リテンション 0%

1週間後に戻ってくるユーザーが**1人もいない**。コホート表で Day 7 が全コホート 0%。

**原因と対応**:
- 「また明日」の動機が無い。通知許可（`notify_prompt` は9件しか出ていない）と復習キュー（`spaced_review_done` 0件）が死んでいる。
- 復習キュー・デイリーボーナス・ストリークを**ホームの先頭**に出す。
- 通知のソフトアスクを「初回ゲーム完了直後」に出し、許可率を上げる。

### 2.3 🔴 app_remove 28%（新規の約3割が削除）

新規228人中63人がアンインストール。初回体験が「面白くない・うるさい・重い」のどれかで切られている。

**原因と対応**:
- 広告が1人406回（バナー自動リフレッシュ）→ **`adShown` を初回ロードのみに修正済み**。表示量を実態に戻す。
- app_exception 1699件 → クラッシュ・例外を直す（下記2.4）。

---

## 3. その他の発見

### 3.1 app_exception 1699件（18人）

1人あたり約94件。Crashlytics で `FlutterError.onError` が拾った未処理例外が大量。

**Crashlytics スタックトレースから判明した重大クラッシュ（別アプリ `marubatusokketu` と同根）**:

```
java.lang.OutOfMemoryError: ... target footprint 201326592 (192MB)
```

- **ExoPlayer（音声）インスタンスのリーク**。`ExoPlayer:Loader:ProgressiveMediaPeriod` スレッドが大量に残っており、BGM/効果音のプレイヤーが `dispose()` されずに増え続けてヒープ枯渇。
- `lib/services/bgm.dart` と `sfx.dart` の音声プレイヤー生成を見直す（1つを持ち回し、切替時は setAsset で差し替え）。

### 3.2 ストーリーモードはデッド

`story_start 1 / story_progress 13 / story_ending 1` ＝1人だけ。Noah ストーリーは事実上使われていない。

**対応済み**: Home のノベル導線を削除、ものがたりタブを撤去（マイページ内の1ボタンへ格下げ）。

### 3.3 ad_shown の水増し

`ad_shown 6095` に対し `ad_load_requested 766`。8倍の差は**バナー広告の自動リフレッシュ（約60秒ごと）で `onAdLoaded` が再発火**していたため。

**対応済み**: `banner_ad_slot.dart` で `_isLoaded` フラグにより**初回ロード時のみ `adShown` を撃つ**よう修正。

### 3.4 収益が GA4 で見えない

イベント表の「合計収益 $0.00」は、AdMob 収益が GA4 に連携されていないため。**`onPaidEvent` で `ad_impression`（value付き）を送る**必要がある。

---

## 4. 改善計画

### フェーズ1：今すぐ（バグ・離脱潰し）
- [x] ad_shown 水増し修正（初回ロードのみ）
- [x] Home のノベル導線削除・ものがたりタブ撤去
- [x] 大きな「あそびかた」ボタンを Home に追加
- [x] 初回起動は即ゲーム開始（既存フロー維持）
- [ ] **音声プレイヤー（ExoPlayer）リークの修正**（OOM の根本）
- [ ] 広告収益を GA4 に連携（`onPaidEvent`）

### フェーズ2：次（初回体験・リテンション）
- [ ] デイリーボーナス・ストリークをホームの先頭へ
- [ ] 通知ソフトアスクを初回ゲーム完了直後に出す
- [ ] 復習キュー（spaced review）をホームに常設

### フェーズ3：育てる（収益・習慣）
- [ ] リワード広告の導線拡充（デイリーボーナス2倍など）
- [ ] 週間レポート通知（「今週は◯人覚えました」）

---

## 5. KPI 目標

| 指標 | 現在 | 1ヶ月目標 |
|---|---|---|
| 起動→初回ゲーム転換 | 26% | 50% |
| Day 7 リテンション | 0% | 8% |
| app_remove | 28% | 15% |
| app_exception | 1699件/月 | <100件/月 |
| 月間広告収益 | 計測不能 | 計測できる状態にする |
