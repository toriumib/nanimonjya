# UI刷新・改名タスク（指示メモ）

このファイルはユーザーからの指示を原文の意図どおりに保存したもの。上から順に実装する。

## 1. 「なまえコール」→「なまえがお」へ改名
検索流入を増やす（検索独占）狙い。アプリ内の表示名・タブ名・l10n・ストア文言を洗い替える。
※ Play の内部パッケージID `com.nanimonjya` は互換維持のため変更しない。

## 2. 顔メモ（アバター）のWeb対応
Web版では Cookie（またはブラウザの永続ストレージ）に保存する。現在モバイル限定になっている保存処理をWebでも動くようにする。

## 3. 顔メモの表現力アップ
- 性別によって「女らしさ／男らしさ」を少し加える
- 髪型の種類を増やす
- 身長・年齢に応じて全身の描画を変える（背を高く／低く、老けさせる）

## 4. チュートリアルで物語モードを紹介する

## 5. 「物語モード」と「読み物」の呼び方が逆になっているので直す

## 6. ホーム画面「ルール／ルールブック」が重複していて変
→ 「ルール」ひとつにまとめる

## 7. はなちゃん・ななちゃんの一言が大きすぎる
→ 小さめの表示にする

## 8. 不要な表示を削る
- 「一人前」みたいな称号 → コメントアウト
- 「今週末はまだ0人」みたいな表示 → 削除

## 9. ホーム画面を一画面にまとめる
以下を綺麗に1画面へ集約する:
- みんなで
- ひとりで
- オンライン ランクマッチ
- 顔メモ → **「会社・学校の人」に改称**（下記14）
- マイページ
- チュートリアル
- 開発者を応援する

## 10. 「おてつき　にまいとも没収」という文言が意味不明
→ 変な文言を入れない。分かりやすい表現にする。

## 11. BGM
- 基本は魔王魂の曲でよい
- **選択した瞬間にその曲へ切り替わるようにする**
- 現状、所持している曲が順番に流れてしまっている疑いがあるので確認する

## 12. 開発者モードを一番下に移動

## 13. 読み物を目次化して、目次から読めるようにする

---

## 14. CPU戦では人数・枚数を選ばせない
難易度ごとに「覚える人数」と「持ち時間」が `models/cpu_difficulty.dart` で
決まっているため、別の人数設定を重ねると報酬とつり合わなくなる。
CPU戦で選ぶのは難易度ひとつだけにする。
（人数・枚数のスライダーは「みんなで（1台）」のシートにだけ置く）

## 15. 「顔メモ」を用途が分かる名前にする
「顔メモ」だけでは何のための機能か分からない。
**会社や学校で新しく会った人を覚えるためのもの**だと名前で伝える。

| 場所 | 変更後 |
|---|---|
| ホームのタイル | 会社・学校の人 |
| 画面タイトル（`customTitle`） | 📇 会社・学校の人をおぼえる |
| タブ名（`tabMemorize`） | 会社・学校の人 |
| ビジネス特訓のボタン | 🧑‍🎨 会社・学校の人を似顔絵でおぼえる |
| 登録シートの項目 | 似顔絵をつくる（写真が無いとき） |

---

## 補足
- ストーリーモード（ADV化・セーブ機能）は**この一覧を片付けた後**に着手する。
- 実装後、Webにデプロイして確認用リンクを出す（手順は `DEPLOY.md`）。
