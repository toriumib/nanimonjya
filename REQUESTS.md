# 依頼と要件のまとめ

オーナーからの依頼と、それを受けて決めたこと・実装したこと・
まだ残っていることを1か所に置く。

⚠️ **判断の理由も残す。** 「なぜそうしたか」が無いと、あとで
同じところを行き来する（実際、BGMの既定値と完走率の設定で起きた）。

最終更新: 2026-08-09

---

## 1. 済んだもの

### 1.1 表示名の統一

| 項目 | 決めたこと |
|---|---|
| アプリ名 | **なまえがお** |
| メインモード | **なまえコール** |

⚠️ 「なづけコール」「ペタネーム」「名前を覚えよう」が混在していた。
`android:label`・ストア掲載名・アプリ内をすべて「なまえがお」にそろえた。
モード名も「なづけコール」→「なまえコール」に統一。

### 1.2 初回起動を「読ませる」から「遊ばせる」へ

計測で **起動162人 → ゲーム開始75人**。半分が遊ぶ前に消えていた。

- おためしを先に出し、遊び終わってから「読みますか？」と一度だけ聞く
- 起動時にOSの通知許可を求めるのをやめた
  （`PushService.init()` が呼んでいた。Androidは一度断られると二度と出せない）
- おためしの出口を `pop` に変更
  （`pushAndRemoveUntil` で HomeShell を作り直していたため、
  遊び終えた直後に全11ページの説明が問答無用で開いていた）

### 1.3 クラッシュの正体

Crashlytics のクラッシュのほぼ全部が **google_fonts が圏外で
fonts.gstatic.com にフォントを取りにいって失敗した**もの。
呼び出しは1か所（ホームのロゴ）だけで、全員が通るので毎回起きていた。

⚠️ **アプリは落ちていない。** `recordFlutterFatalError` にしていたので
分類が「致命的」だっただけ。クラッシュ率66%は見かけの数字。
→ `recordFlutterError`（非致命的）に変更。
→ 「消えた試合102件」はクラッシュではなく**飽きて外に出たまま戻らない**。

対策: ロゴのフォントを同梱し、`google_fonts` の依存自体を外した。

### 1.4 ゲーム設定

| 項目 | 値 |
|---|---|
| 登場人数 | **12人**（4〜15で変更可） |
| 1人あたりの枚数 | **5枚**（2〜5で変更可） |
| 山札 | **60枚** |

⚠️ **完走率とのトレードオフを承知のうえで戻した値。**
もともと12人だったが、計測で完走37%と分かって 6→4人と下げた経緯がある。
2026-08 時点の完走率39%（目標60%）。下げたくなったら
`name_call.dart` の `peopleCount` だけ直せばよい。

### 1.5 手ざわり（なまえコール）

唯一まともに遊ばれているモードなので、ここに投資する。

- 山札の進捗バー「のこり◯枚」（60枚あるのに終わりが見えなかった）
- 連続正解バッジ。2連続から出し、5連続で色と文言が変わる
- カード獲得の帯「カードゲット！」「P1 が正解！」
- 紙吹雪＋フラッシュ＋「クリア！」（**1.5秒で終わらせる**）
- 顔写真を拡大。命名のときと想起のときで**同じ大きさ**にそろえた
- P1/P2 ボタンを大きく。2人までは1列にして1つを大きく取る
- 「だれも思い出せなかった」を28px離し、400msの連打よけを入れた
  （連打すると名前が振り直されるのがいちばん困る誤操作だった）
- 「名簿公開！おぼえてた？」を飛ばして、みんなで／オンラインは
  そのまま結果画面へ

### 1.6 フォント

| 用途 | 書体 | 大きさ |
|---|---|---|
| 本文 | Zen Maru Gothic | 全字（7.6MB） |
| ロゴ | Mochiy Pop One | **32KB**（絞りこみ） |
| お祝い | Dela Gothic One | **37KB**（絞りこみ） |

⚠️ **出す文言を変えたら `tools/subset_logo_font.py` を回し直す。**
入っていない字は豆腐（□）になる。実際「！」が全角で抜けていた。

### 1.7 音楽（38MB → 21MB）

**削除した8曲**: ノクターン／練習曲Op.10-4／エリーゼのために／歓喜の歌／
バーニングハート／アイネ・クライネ／ハルジオン／test_bgm

⚠️ **削除対象が既定値になっていた。** そのまま消していれば全員が無音。
`migrateBgmSelection()` で救う。純粋関数にしてテストを書いてある。

| 場面 | 既定 | 動き |
|---|---|---|
| ホーム | おまかせ | シチリアーノ／運命をランダム。**鳴っている曲は続ける** |
| 試合中 | シチリアーノ | — |
| 勝利 | おまかせ | 勝利の行進曲4曲からランダム。**毎回引き直す** |

**設定で1曲を選べば、その曲だけが鳴る。**

### 1.8 効果音

`tools/gen_sfx.py` で**合成**する。配布素材を拾うと出所の管理が増えるため。
get / correct / victory / fanfare を作り直した。

⚠️ **開発者は音を聴けない。** `--variants` で3案ずつ出して選ぶ。
現在は案0。差し替えは `--pick get=1,victory=2`。

### 1.9 計測

`ANALYTICS_REVIEW.md` に所見と計画。`tools/analyze_reports.py` で
CSVから毎週見る数字を自動集計できる。

追加したイベント: 顔メモ／よみもの／記事の読了率／名刺覚え／
広告のロードと表示／`first_game_started`／`game_background`（飽きと
クラッシュの切り分け）／デッキ・オンライン・ものがたりの入口。

---

## 2. ⚠️ 未解決（判断待ち）

### 2.1 勝利BGMのライセンス

| 曲 | 楽曲 | 録音 | 判定 |
|---|---|---|---|
| 威風堂々 | Elgar（PD） | United States Army Band | ✅ 米国政府作成物・PD |
| 義勇軍進行曲 | 聶耳（PD-China） | United States Navy Band | ✅ PD US Navy |
| 軍艦マーチ | 瀬戸口藤吉（PD） | 海上自衛隊東京音楽隊 | ⚠️ 下記 |
| 運命（勝利版） | Beethoven（PD） | `-2019-AR2` | ❓ 出所不明 |

**軍艦マーチ**: 出所は海上自衛隊東京音楽隊のダウンロードページ。
防衛省サイトは通常**政府標準利用規約（第2.0版）**に準拠しており、
商用利用可・**出典表示が必要**。
⚠️ ただし当該ページを直接確認できていない（403）。
**ページ内の利用条件を目で確認すること。**

**運命（勝利版）**: 出所不明。楽曲はPDだが録音には別の権利がある。
入手元が分かれば確認する。分からなければ、すでに同梱している
`beethoven_5th.mp3`（Skidmore College orchestra・PD）から作り直せばよい。

### 2.2 義勇軍進行曲を入れるかどうか

著作権上は問題なし（上記）。**別の論点として**、中国には国歌法（2017年）が
あり、商業広告や公共の場のBGMなど「ふさわしくない場面」での使用を
禁じている。ゲームの勝利ジングルがこれに当たるかは判断が要る。

外すなら `kVictoryRandomPool` から1行消すだけ。

### 2.3 効果音の選定

12案を書き出し済み（`tools/sfx_preview/`）。聴いて選ぶ。

### 2.4 GA4 側の設定（コンソール作業・オーナーのみ可能）

1. **内部トラフィックの除外**（開発端末が混ざっている。滞在3,110秒の日がある）
2. **カスタムディメンションの登録**（`mode` `feature` `kind` `format` など）
   登録しないとパラメータで絞り込めない。**登録日以降のデータにしか効かない**
3. キーイベントの追加
4. BigQuery エクスポート

### 2.5 アプリアイコン

「なまえコール」時代の名刺カードに戻してある。
⚠️ フリー素材のモデル写真は「イメージキャラクター・公式イメージとしての
利用」が原則禁止。写真を使うなら素材元の承諾書が要る。

---

## 3. 守ること

- **他ボードゲームを想起させる名称・文言・イラストを使わない**
- 医療・認知機能への効果を断定しない（「〜と言われています」＋出典）
- 記憶術の文章はオリジナル。外部記事のコピー禁止
- `dart:io` の `Platform` を使わない（Webでクラッシュ）。`kIsWeb` を使う
- 広告関連は `kIsWeb` ガード必須
- versionCode は `scripts/bump_and_build.ps1` で上げる
- Web は **`build/web` の中で** `npx vercel --prod`
  （ルートから叩くとファイル数上限15,000に引っかかる）
- 曲・フォント・素材を足したら、**出所とライセンスをコードのコメントに書く**

---

## 4. 関連ファイル

| 目的 | 場所 |
|---|---|
| 計測の所見と計画 | `ANALYTICS_REVIEW.md` |
| 計測の実装リファレンス | `ANALYTICS.md` |
| ストア掲載文 | `STORE_LISTING.md` |
| ビルドとリリース | `DEPLOY.md` |
| スマホから作業 | `MOBILE.md` |
| レポート集計 | `tools/analyze_reports.py` |
| フォント絞りこみ | `tools/subset_logo_font.py` |
| 効果音の合成 | `tools/gen_sfx.py` |
| 勝利BGMの切り出し | `tools/make_victory_bgm.py` |
| 運命の切り出し | `tools/make_bgm_unmei.py` |
