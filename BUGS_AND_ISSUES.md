# コード監査レポート（バグ・エラー・不自然な単語）

> 調査日: 2026-08-20。`flutter analyze` ・未配線イベント・ハードコード文言・既知の懸念の点検結果。
> 修正はまだ行っていない（判断はあなたに）。

---

## 1. ビルド・デプロイの状態（正常）

| 項目 | 状態 |
|---|---|
| AAB v215 | ✅ ビルド済み（`build/app/outputs/bundle/release/app-release.aab`） |
| Web | ✅ Vercel 本番デプロイ済み（https://web-sigma-drab-72.vercel.app） |
| `flutter analyze` | エラー0・警告5件 |
| `flutter test` | 全295件通過 |

---

## 2. `flutter analyze` の警告（デッドコード5件）

修正するだけで消える軽いもの。

| 箇所 | 内容 |
|---|---|
| `top_screen.dart:210` `_gradientButton` | 未使用メソッド |
| `top_screen.dart:1267` `_loginCharProgress` | 未使用メソッド |
| `tutorial_screen.dart:242` `_difficultyTable` | 未使用メソッド |
| `tutorial_screen.dart:102` `extra` パラメータ | 誰も渡さない |
| `feat_character_test.dart:53` `newly` | テスト内で未使用ローカル変数 |

---

## 3. ハードコード日本語（英語版で日本語が表示される）

**要修正**: `ja` 分岐なしで直接日本語を `Text()` に出している。

| 箇所 | 文言 | 画面 |
|---|---|---|
| `avatar_editor_screen.dart:90` | 「この人の特徴」 | アバター編集 |
| `avatar_editor_screen.dart:235` | 「この顔で決定」 | アバター編集 |
| `custom_roster_screen.dart:189` | 「似顔絵をつくる（写真が無いとき）」 | 顔メモ |
| `custom_roster_screen.dart:297` | 「似顔絵をつくる」 | 顔メモ |
| `custom_roster_screen.dart:797` | 「顔を直す」 | 顔メモ |
| `article_library_screen.dart:306` | 「・$s」 | 記事（日本語の読点リーダー） |

**問題なし**（固有名詞・絵文字）: 「魔王魂」（BGMクレジット）、🪙・⚡・✅ など。

---

## 4. 未配線の Analytics イベント（計測の穴）

定義済みだが、どこからも呼ばれていない。

| イベント | 何を測るはず |
|---|---|
| `deckToggle` | デッキの出演ON/OFF切替 |
| `onlineLobbyLeave` | オンラインロビーを離脱（待ち時間付き） |
| `tapAction` | 画面内のボタン押下 |
| `settingChange` | 設定変更（人数・テーマ・BGM等） |

> `settingChange` は以前トップ画面で使っていたが、リニューアルで消えた。

---

## 5. 既知の懸念（修正・確認待ち）

| 項目 | 状況 |
|---|---|
| **BGM が鳴らない（エミュレータ）** | コードは正しい。エミュレータのオーディオ出力に依存する可能性が高い。**実機で確認** |
| **BGM がランダムに聞こえる** | ホームの曲が「おまかせ（ランダム）」設定のため。マイページ→曲選び（ホーム）で固定に変更可 |
| **コイン現金購入が「準備中」** | エミュレータはストア未接続のため。Play Console 商品登録済みなら**実機では価格が表示される** |
| **OOM クラッシュ（ExoPlayer リーク）** | 別アプリで確認。nanimonjya も `just_audio` を使うので、音声プレイヤーの使い回しを要監査（`NEXT_STEPS.md` ステップ1参照） |

---

## 6. おすすめの修正順

1. **ハードコード日本語（6箇所）** — 英語版ユーザーに日本語が見えるのは明確な不具合。MetaStrings に追加して `ja` 分岐に
2. **analyze の警告5件** — デッドコード削除で警告ゼロに
3. **未配線イベント** — 配線するか、使わないなら定義を削除（`tapAction` は任意）
4. **BGM・OOM の実機確認** — 機種・手順を教えてもらえれば調査

---

## 7. その他メモ

- 名称は「**なまえがお**」（タブ・アプリ名）と「**なまえコール**」（ゲームモード名）の2つが意図的に併用されている。統一したい場合はどちらかに寄せる
- 英語の長文（オンライン・ルール説明）はすべて英訳済みで、翻訳漏れは確認されなかった
